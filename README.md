# BlueDart Live Tracer

A Flask web application to track BlueDart shipment IDs and export results to Excel.

## Features

- Single tracking ID support
- Bulk tracking ID support
- Excel download
- Render deployment ready
- API-based tracking structure to avoid website blocking

## Setup

Create virtual environment:

```powershell
python -m venv .venv